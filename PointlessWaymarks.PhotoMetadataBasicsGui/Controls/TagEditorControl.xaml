<UserControl x:Class="PointlessWaymarks.PhotoMetadataBasicsGui.Controls.TagEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PointlessWaymarks.PhotoMetadataBasicsGui.Controls"
             xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance local:TagsEditorContext}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <Label
                    Content="Tags (Comma,Separated)"
                    Style="{StaticResource DataEntryLabelStyle}"
                    Target="{Binding ElementName=TagsTextBox}"
                    ToolTip="{Binding HelpText}" />
                <Image
                    Width="{StaticResource SmallIconSize}"
                    Margin="0,0,4,0"
                    VerticalAlignment="Center"
                    Source="{svgc:SvgImage Source=/Icons/information-outline.svg,
                                           AppName=PointlessWaymarks.WpfCommon}"
                    Visibility="{Binding HasChanges, Converter={StaticResource BooleanToVisibility}}">
                    <Image.ToolTip>
                        <ToolTip Content="{Binding TagsReferenceString, FallbackValue='Previously blank', TargetNullValue='Previously empty'}" ContentStringFormat="Original Value: {0}" />
                    </Image.ToolTip>
                </Image>
                <Image
                    Width="{StaticResource SmallIconSize}"
                    VerticalAlignment="Center"
                    Source="{svgc:SvgImage Source=/Icons/alert-octagon.svg,
                                           AppName=PointlessWaymarks.WpfCommon}"
                    ToolTip="{Binding TagsValidationMessage}"
                    Visibility="{Binding HasValidationIssues, Converter={StaticResource BooleanToVisibility}}" />
            </StackPanel>
        </StackPanel>
        <TextBox
            Grid.Row="1"
            Padding="4"
            Name="TagsTextBox"
            Style="{StaticResource DataEntryTextBoxMultiLineStyle}"
            Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged, Delay=250}" />
    </Grid>
</UserControl>
